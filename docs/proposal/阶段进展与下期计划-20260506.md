# 毕业设计阶段进展与下期计划（2026-05-06）

## 1. 当前进度

本阶段已完成“可复现实验流水线”主干打通，重点结果如下：

1. 完成实验自动化入口脚本：
   - `scripts/run_experiments.ps1`
   - 支持按策略自动执行：部署、预热、压测、采集、归档、汇总、出图。
2. 完成四组策略文件落地：
   - `static`（固定副本）
   - `hpa_cpu`
   - `hpa_multi`（CPU+Memory+QPS）
   - `hpa_vpa`（当前集群无VPA CRD时自动降级并给出告警）
3. 完成数据处理与质检：
   - `results/aggregate_experiment.py`
   - 包含缺列、空值、异常值、重复次数等校验逻辑。
4. 完成图表脚本增强：
   - `results/plot_results.py`
   - 支持均值+标准差误差线图输出。
5. 完成 Minikube+Docker+代理链路打通与冒烟测试：
   - `minikube2` 集群已启动并可用；
   - `kubectl top nodes` 可正常返回；
   - 已完成 4 组策略各 1 次（2 分钟）冒烟压测并生成结果。

## 2. 本阶段产出物

1. 配置与脚本：
   - `scripts/experiment.config.json`
   - `scripts/experiment.smoke.json`
   - `scripts/run_experiments.ps1`
2. 策略与部署：
   - `deploy/base/deployment.yaml`（镜像源可拉取化）
   - `deploy/hpa/hpa-multi.yaml`
   - `deploy/vpa/vpa-initial.yaml`
3. 结果与图表：
   - `results/processed/experiment_metrics.csv`
   - `results/processed/experiment_metrics_summary.csv`
   - `results/figures/*.png`

## 3. 现存问题与说明

1. 当前集群尚未安装 VPA CRD 与控制器，因此 `hpa_vpa` 组暂以“自动降级+告警”方式执行，不等同于完整协同实验。
2. 个别 run 的 `top` 采样受环境波动影响，汇总脚本已加入兜底与标识字段（`top_sample_fallback`）以保证流程可重跑不中断。

## 4. 下期计划（建议 7 天内完成）

1. 安装并验证 VPA 组件（CRD + Recommender + Updater + Admission）。
2. 运行正式实验：`4 组 × 3 次 × 5 分钟`（主场景 burst）。
3. 生成正式版论文图表与结论表：
   - P95/P99、QPS、失败率；
   - CPU/内存利用率、伸缩事件、振荡事件；
   - 成本代理指标。
4. 形成“可复现说明”与“实验记录清单”（含运行参数、环境信息、commit id）。
5. 将正式实验结论回填论文实验章节与答辩 PPT。

## 5. 下期验收标准

1. `results/raw` 中每个策略存在 3 份完整 run 目录。
2. `results/processed/experiment_metrics.csv` 含 12 条有效记录（4×3）。
3. `results/figures/` 产出完整图集且无缺失指标。
4. `hpa_vpa` 组为真实 VPA 协同结果（非降级执行）。
