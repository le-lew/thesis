apiVersion: v1
kind: Service
metadata:
  name: sample-api
  namespace: thesis-demo
  labels:
    app: sample-api
spec:
  selector:
    app: sample-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
